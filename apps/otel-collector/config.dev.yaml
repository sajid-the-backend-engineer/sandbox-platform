# OpenTelemetry Collector configuration for Northrays

receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318
        include_metadata: true

exporters:
  northrays_exporter:
    # Sandbox authentication header name
    sandbox_auth_token_header: 'sandbox-auth-token'

    # Cache TTL for endpoint configurations
    cache_ttl: 5m

    # Default timeout for OTLP export requests
    default_timeout: 30s

    # Retry settings for failed exports
    retry_on_failure:
      enabled: true
      initial_interval: 5s
      max_interval: 30s
      max_elapsed_time: 5m

    # Northrays API configuration
    api_url: ${env:NORTHRAYS_API_URL:-http://localhost:3000/api}
    api_key: ${env:NORTHRAYS_API_KEY:-otel_collector_api_key}

    # Optional Redis configuration for caching
    redis:
      host: '${env:REDIS_HOST:-redis}'
      port: '${env:REDIS_PORT:-6379}'
      password: '${env:REDIS_PASSWORD:-}'

extensions:
  healthcheckv2:
    use_v2: true
    component_health:
      include_permanent_errors: false
      include_recoverable_errors: true
      recovery_duration: 5m
    http:
      endpoint: '0.0.0.0:13133'
      status:
        enabled: true
        path: '/health/status'
      config:
        enabled: true
        path: '/health/config'
    grpc:
      endpoint: '0.0.0.0:13132'
      transport: 'tcp'

service:
  extensions: [healthcheckv2]
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [northrays_exporter]
    metrics:
      receivers: [otlp]
      exporters: [northrays_exporter]
    logs:
      receivers: [otlp]
      exporters: [northrays_exporter]

#!/bin/bash
set -e

# Reload systemd daemon to remove the service
systemctl daemon-reload

# On purge, remove configuration and data
if [ "$1" = "purge" ]; then
    rm -rf /etc/northrays/runner.env
    rm -rf /var/lib/northrays/runner
    rm -rf /var/log/northrays
fi

exit 0

---
title: CLI
description: A reference of supported operations using the Northrays CLI.
sidebar:
  label: Northrays CLI Reference
---
import { TabItem, Tabs } from "@astrojs/starlight/components";
import Aside from "@components/Aside.astro";
import Label from "@components/Label.astro";

Northrays provides command-line access to core features for interacting with Northrays Sandboxes, including managing their lifecycle, snapshots, and more.

The CLI reference lists all commands supported by the `northrays` command-line tool, complete with a description of their behavior, and any supported flags.
You can access this documentation on a per-command basis by appending the `--help`/`-h` flag when invoking `northrays`.

## Installation

Install the Northrays CLI to interact with Northrays sandboxes from the command line.

<Tabs syncKey="language">
<TabItem label="Mac">

```bash
brew install northrays/cli/northrays
```

Trust the tap once so routine `brew upgrade` keeps the Northrays CLI up to date. Recent Homebrew versions require third-party taps to be explicitly trusted; without it, a bare `brew upgrade` skips the Northrays tap and the CLI goes stale:

```bash
brew trust northrays/cli
```

To upgrade the Northrays CLI to the latest version:

```bash
brew upgrade northrays/cli/northrays
```

Alternatively, install directly without Homebrew:

For Apple Silicon (`arm64`):

  ```bash
  sudo curl -fL https://github.com/northrays/sandbox-platform/releases/latest/download/northrays-darwin-arm64 -o /usr/local/bin/northrays && sudo chmod +x /usr/local/bin/northrays
  ```

For Intel (`amd64`):

  ```bash
  sudo curl -fL https://github.com/northrays/sandbox-platform/releases/latest/download/northrays-darwin-amd64 -o /usr/local/bin/northrays && sudo chmod +x /usr/local/bin/northrays
  ```

</TabItem>
<TabItem label="Linux">

Choose the command for your Linux architecture. Both commands download the latest binary from GitHub releases and install it to `/usr/local/bin`, overwriting any existing version.

For `amd64` (`x86_64`):

  ```bash
  sudo curl -fL https://github.com/northrays/sandbox-platform/releases/latest/download/northrays-linux-amd64 -o /usr/local/bin/northrays && sudo chmod +x /usr/local/bin/northrays
  ```

For `arm64` (`aarch64`):

  ```bash
  sudo curl -fL https://github.com/northrays/sandbox-platform/releases/latest/download/northrays-linux-arm64 -o /usr/local/bin/northrays && sudo chmod +x /usr/local/bin/northrays
  ```

</TabItem>
<TabItem label="Windows">

```bash
powershell -Command "irm https://get.northrays.com/windows | iex"
```

</TabItem>
</Tabs>

After installing the Northrays CLI, use the `northrays` command to interact with Northrays sandboxes from the command line.

## northrays
Northrays CLI

```shell
northrays [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |
| `--version` | `-v` | Display the version of Northrays |


## northrays archive
Archive a sandbox

```shell
northrays archive [SANDBOX_ID] | [SANDBOX_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays autocomplete
Adds a completion script for your shell environment

```shell
northrays autocomplete [bash|zsh|fish|powershell] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |

<Aside type="note">
If using bash shell environment, make sure you have bash-completion installed in order to get full autocompletion functionality.
Linux Installation: ```sudo apt-get install bash-completion```
macOS Installation: ```brew install bash-completion```
</Aside>

## northrays create
Create a new sandbox

```shell
northrays create [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--auto-archive` |  | Auto-archive interval in minutes (0 means the maximum interval will be used) |
| `--auto-delete` |  | Auto-delete interval in minutes (negative value means disabled, 0 means delete immediately upon stopping) |
| `--auto-stop` |  | Auto-stop interval in minutes (0 means disabled) |
| `--context` | `-c` | Files or directories to include in the build context (can be specified multiple times) |
| `--cpu` |  | CPU cores allocated to the sandbox |
| `--disk` |  | Disk space allocated to the sandbox in GB |
| `--dockerfile` | `-f` | Path to Dockerfile for Sandbox snapshot |
| `--env` | `-e` | Environment variables (format: KEY=VALUE) |
| `--gpu` |  | GPU units allocated to the sandbox |
| `--label` | `-l` | Labels (format: KEY=VALUE) |
| `--memory` |  | Memory allocated to the sandbox in MB |
| `--name` |  | Name of the sandbox |
| `--network-allow-list` |  | Comma-separated list of allowed CIDR network addresses for the sandbox |
| `--network-block-all` |  | Whether to block all network access for the sandbox |
| `--public` |  | Make sandbox publicly accessible |
| `--snapshot` |  | Snapshot to use for the sandbox |
| `--target` |  | Target region (eu, us) |
| `--user` |  | User associated with the sandbox |
| `--volume` | `-v` | Volumes to mount (format: VOLUME_ID_OR_NAME:MOUNT_PATH) |
| `--help` |  | help for northrays |


## northrays delete
Delete a sandbox

```shell
northrays delete [SANDBOX_ID] | [SANDBOX_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | Delete all sandboxes |
| `--help` |  | help for northrays |


## northrays docs
Opens the Northrays documentation in your default browser.

```shell
northrays docs [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays exec
Execute a command in a sandbox

```shell
northrays exec [SANDBOX_ID | SANDBOX_NAME] -- [COMMAND] [ARGS...] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--cwd` |  | Working directory for command execution |
| `--timeout` |  | Command timeout in seconds (0 for no timeout) |
| `--help` |  | help for northrays |


## northrays info
Get sandbox info

```shell
northrays info [SANDBOX_ID] | [SANDBOX_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | Output format. Must be one of (yaml, json) |
| `--help` |  | help for northrays |


## northrays list
List sandboxes

```shell
northrays list [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--cursor` | `-c` | Cursor for pagination |
| `--format` | `-f` | Output format. Must be one of (yaml, json) |
| `--limit` | `-l` | Maximum number of items per page |
| `--help` |  | help for northrays |


## northrays login
Log in to Northrays

```shell
northrays login [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--api-key` |  | API key to use for authentication |
| `--help` |  | help for northrays |


## northrays logout
Logout from Northrays

```shell
northrays logout [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays mcp
Manage Northrays MCP Server

```shell
northrays mcp [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays mcp config
Outputs JSON configuration for Northrays MCP Server

```shell
northrays mcp config [AGENT_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays mcp init
Initialize Northrays MCP Server with an agent (currently supported: claude, windsurf, cursor)


```shell
northrays mcp init [AGENT_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays mcp start
Start Northrays MCP Server

```shell
northrays mcp start [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays organization
Manage Northrays organizations

```shell
northrays organization [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays organization create
Create a new organization and set it as active

```shell
northrays organization create [ORGANIZATION_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays organization delete
Delete an organization

```shell
northrays organization delete [ORGANIZATION] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays organization list
List all organizations

```shell
northrays organization list [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | Output format. Must be one of (yaml, json) |
| `--help` |  | help for northrays |


## northrays organization use
Set active organization

```shell
northrays organization use [ORGANIZATION] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays preview-url
Get signed preview URL for a sandbox port

```shell
northrays preview-url [SANDBOX_ID | SANDBOX_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--expires` |  | URL expiration time in seconds |
| `--port` | `-p` | Port number to get preview URL for (required) |
| `--help` |  | help for northrays |


## northrays snapshot
Manage Northrays snapshots

```shell
northrays snapshot [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays snapshot create
Create a snapshot

```shell
northrays snapshot create [SNAPSHOT] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--context` | `-c` | Files or directories to include in the build context (can be specified multiple times). If not provided, context will be automatically determined from COPY/ADD commands in the Dockerfile |
| `--cpu` |  | CPU cores that will be allocated to the underlying sandboxes (default: 1) |
| `--disk` |  | Disk space that will be allocated to the underlying sandboxes in GB (default: 3) |
| `--dockerfile` | `-f` | Path to Dockerfile to build |
| `--entrypoint` | `-e` | The entrypoint command for the snapshot |
| `--image` | `-i` | The image name for the snapshot |
| `--memory` |  | Memory that will be allocated to the underlying sandboxes in GB (default: 1) |
| `--region` |  | ID of the region where the snapshot will be available (defaults to organization default region) |
| `--help` |  | help for northrays |


## northrays snapshot delete
Delete a snapshot

```shell
northrays snapshot delete [SNAPSHOT_ID | SNAPSHOT_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--all` | `-a` | Delete all snapshots |
| `--help` |  | help for northrays |


## northrays snapshot list
List all snapshots

```shell
northrays snapshot list [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | Output format. Must be one of (yaml, json) |
| `--limit` | `-l` | Maximum number of items per page |
| `--page` | `-p` | Page number for pagination (starting from 1) |
| `--help` |  | help for northrays |


## northrays snapshot push
Push local snapshot

```shell
northrays snapshot push [SNAPSHOT] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--cpu` |  | CPU cores that will be allocated to the underlying sandboxes (default: 1) |
| `--disk` |  | Disk space that will be allocated to the underlying sandboxes in GB (default: 3) |
| `--entrypoint` | `-e` | The entrypoint command for the image |
| `--memory` |  | Memory that will be allocated to the underlying sandboxes in GB (default: 1) |
| `--name` | `-n` | Specify the Snapshot name |
| `--region` |  | ID of the region where the snapshot will be available (defaults to organization default region) |
| `--help` |  | help for northrays |


## northrays ssh
SSH into a sandbox

```shell
northrays ssh [SANDBOX_ID] | [SANDBOX_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--expires` |  | SSH access token expiration time in minutes (defaults to 24 hours) |
| `--help` |  | help for northrays |


## northrays start
Start a sandbox

```shell
northrays start [SANDBOX_ID] | [SANDBOX_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays stop
Stop a sandbox

```shell
northrays stop [SANDBOX_ID] | [SANDBOX_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--force` | `-f` | Force stop the sandbox using SIGKILL |
| `--help` |  | help for northrays |


## northrays version
Print the version number

```shell
northrays version [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays volume
Manage Northrays volumes

```shell
northrays volume [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays volume create
Create a volume

```shell
northrays volume create [NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--size` | `-s` | Size of the volume in GB |
| `--help` |  | help for northrays |


## northrays volume delete
Delete a volume

```shell
northrays volume delete [VOLUME_ID_OR_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--help` |  | help for northrays |


## northrays volume get
Get volume details

```shell
northrays volume get [VOLUME_ID_OR_NAME] [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | Output format. Must be one of (yaml, json) |
| `--help` |  | help for northrays |


## northrays volume list
List all volumes

```shell
northrays volume list [flags]
```

__Flags__
| Long | Short | Description |
| :--- | :---- | :---------- |
| `--format` | `-f` | Output format. Must be one of (yaml, json) |
| `--help` |  | help for northrays |



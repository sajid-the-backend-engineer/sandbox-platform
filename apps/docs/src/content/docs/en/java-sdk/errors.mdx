---
title: "Errors"
hideTitleOnPage: true
---

## NorthraysException

Base exception for all Northrays SDK errors.

Subclasses map to specific HTTP status codes and allow callers to catch
precise failure conditions without string-parsing error messages:
```java
try {
Sandbox sandbox = northrays.sandbox().get("nonexistent-id");
} catch (NorthraysNotFoundException e) {
// sandbox does not exist
} catch (NorthraysAuthenticationException e) {
// invalid API key
} catch (NorthraysException e) {
// other SDK error
}
```

### Constructors

#### new NorthraysException()
```java
public NorthraysException(String message)
```

Creates a generic Northrays exception.

**Parameters**:

- `message` _String_ - error description

#### new NorthraysException()
```java
public NorthraysException(String message, Throwable cause)
```

Creates a generic Northrays exception with a cause.

**Parameters**:

- `message` _String_ - error description
- `cause` _Throwable_ - root cause

#### new NorthraysException()
```java
public NorthraysException(int statusCode, String message)
```

Creates a Northrays exception with explicit HTTP status code.

**Parameters**:

- `statusCode` _int_ - HTTP status code
- `message` _String_ - error description

#### new NorthraysException()
```java
public NorthraysException(int statusCode, String message, Throwable cause)
```

Creates a Northrays exception with explicit HTTP status code and a cause.

**Parameters**:

- `statusCode` _int_ - HTTP status code
- `message` _String_ - error description
- `cause` _Throwable_ - root cause

#### new NorthraysException()
```java
public NorthraysException(int statusCode, String message, Map<String, String> headers)
```

Creates a Northrays exception with HTTP status code and headers.

**Parameters**:

- `statusCode` _int_ - HTTP status code
- `message` _String_ - error description
- `headers` _Map\<String, String\>_ - response headers

### Methods

#### getStatusCode()
```java
public int getStatusCode()
```

Returns the HTTP status code, or 0 if not applicable.

**Returns**:

- `int` -

#### getHeaders()
```java
public Map<String, String> getHeaders()
```

Returns the HTTP response headers, or an empty map if not available.

**Returns**:

- `Map\<String, String\>` -

## NorthraysAuthenticationException

Raised when API credentials are missing or invalid (HTTP 401).
```java
try {
northrays.sandbox().create();
} catch (NorthraysAuthenticationException e) {
System.err.println("Invalid or missing API key");
}
```

### Constructors

#### new NorthraysAuthenticationException()
```java
public NorthraysAuthenticationException(String message)
```

Creates an authentication exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NorthraysAuthenticationException()
```java
public NorthraysAuthenticationException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NorthraysBadRequestException

Raised when the request is malformed or contains invalid parameters (HTTP 400).
```java
try {
northrays.sandbox().create(params);
} catch (NorthraysBadRequestException e) {
System.err.println("Invalid request parameters: " + e.getMessage());
}
```

### Constructors

#### new NorthraysBadRequestException()
```java
public NorthraysBadRequestException(String message)
```

Creates a bad-request exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NorthraysBadRequestException()
```java
public NorthraysBadRequestException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NorthraysConflictException

Raised when an operation conflicts with the current state (HTTP 409).

Common causes: creating a resource with a name that already exists,
or performing an operation incompatible with the resource's current state.
```java
try {
northrays.snapshot().create(params);
} catch (NorthraysConflictException e) {
System.err.println("A snapshot with this name already exists");
}
```

### Constructors

#### new NorthraysConflictException()
```java
public NorthraysConflictException(String message)
```

Creates a conflict exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NorthraysConflictException()
```java
public NorthraysConflictException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NorthraysConnectionException

Raised for network-level connection failures (no HTTP response received).

Raised when the SDK cannot reach the Northrays API due to network issues
such as DNS failure, connection refused, or TLS errors.
```java
try {
northrays.sandbox().create();
} catch (NorthraysConnectionException e) {
System.err.println("Cannot reach Northrays API: " + e.getMessage());
}
```

### Constructors

#### new NorthraysConnectionException()
```java
public NorthraysConnectionException(String message)
```

Creates a connection exception.

**Parameters**:

- `message` _String_ - connection failure description

#### new NorthraysConnectionException()
```java
public NorthraysConnectionException(String message, Throwable cause)
```

Creates a connection exception with a cause.

**Parameters**:

- `message` _String_ - connection failure description
- `cause` _Throwable_ - root cause

## NorthraysForbiddenException

Raised when the authenticated user lacks permission to perform an operation (HTTP 403).
```java
try {
northrays.sandbox().delete(sandboxId);
} catch (NorthraysForbiddenException e) {
System.err.println("Not authorized to delete this sandbox");
}
```

### Constructors

#### new NorthraysForbiddenException()
```java
public NorthraysForbiddenException(String message)
```

Creates a forbidden exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NorthraysForbiddenException()
```java
public NorthraysForbiddenException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NorthraysNotFoundException

Raised when a requested resource does not exist (HTTP 404).

### Constructors

#### new NorthraysNotFoundException()
```java
public NorthraysNotFoundException(String message)
```

Creates a not-found exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NorthraysNotFoundException()
```java
public NorthraysNotFoundException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NorthraysRateLimitException

Raised when API rate limits are exceeded (HTTP 429).

### Constructors

#### new NorthraysRateLimitException()
```java
public NorthraysRateLimitException(String message)
```

Creates a rate-limit exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NorthraysRateLimitException()
```java
public NorthraysRateLimitException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NorthraysServerException

Raised for unexpected server-side failures (HTTP 5xx).

These are typically transient and safe to retry with exponential backoff.
```java
try {
northrays.sandbox().create();
} catch (NorthraysServerException e) {
System.err.println("Server error (status " + e.getStatusCode() + "), retry later");
}
```

### Constructors

#### new NorthraysServerException()
```java
public NorthraysServerException(int statusCode, String message)
```

Creates a server exception.

**Parameters**:

- `statusCode` _int_ - HTTP status code (typically 5xx)
- `message` _String_ - error description from the API

#### new NorthraysServerException()
```java
public NorthraysServerException(int statusCode, String message, Throwable cause)
```

**Parameters**:

- `statusCode` _int_ - HTTP status code (typically 5xx)
- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NorthraysTimeoutException

Raised when an SDK operation times out.

This exception is generated client-side and is not tied to a single HTTP status code.

### Constructors

#### new NorthraysTimeoutException()
```java
public NorthraysTimeoutException(String message, Throwable cause)
```

Creates a timeout exception with a cause.

**Parameters**:

- `message` _String_ - timeout description
- `cause` _Throwable_ - root cause

#### new NorthraysTimeoutException()
```java
public NorthraysTimeoutException(String message)
```

Creates a timeout exception.

**Parameters**:

- `message` _String_ - timeout description

## NorthraysValidationException

Raised for semantic validation failures (HTTP 422).

Raised when the request is well-formed but the values fail business logic
validation (e.g., unsupported resource class, invalid configuration).
```java
try {
northrays.sandbox().create(params);
} catch (NorthraysValidationException e) {
System.err.println("Validation failed: " + e.getMessage());
}
```

### Constructors

#### new NorthraysValidationException()
```java
public NorthraysValidationException(String message)
```

Creates a validation exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NorthraysValidationException()
```java
public NorthraysValidationException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

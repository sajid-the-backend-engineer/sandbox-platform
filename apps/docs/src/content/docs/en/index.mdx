---
title: Northrays Documentation
description: Start managing your Sandboxes with Northrays.
template: doc
head:
  - tag: title
    content: Documentation · Northrays
  - tag: meta
    attrs:
      property: og:title
      content: Documentation · Northrays
  - tag: meta
    attrs:
      name: twitter:title
      content: Documentation · Northrays
tableOfContents: true
---

import { TabItem, Tabs } from '@astrojs/starlight/components'

Northrays is a secure and elastic infrastructure for running AI-generated code. Northrays provides full composable computers — [sandboxes](/docs/en/sandboxes) — with complete isolation, a dedicated kernel, filesystem, network stack, and allocated vCPU, RAM, and disk.

Sandboxes are the core component of the Northrays platform, spinning up in under 90ms from code to execution and running any code in Python, TypeScript, and JavaScript. Built on OCI/Docker compatibility, massive parallelization, and unlimited persistence, sandboxes deliver consistent, predictable environments for agent workflows.

Agents and developers interact with sandboxes programmatically using the Northrays SDKs, API, and CLI. Operations span sandbox lifecycle management, filesystem operations, process and code execution, and runtime configuration. Our stateful environment snapshots enable persistent agent operations across sessions, making Northrays the ideal foundation for AI agent architectures.

- **Northrays SDKs**: [TypeScript](/docs/en/typescript-sdk), [Python](/docs/en/python-sdk), [Ruby](/docs/en/ruby-sdk), [Go](/docs/en/go-sdk), [Java](/docs/en/java-sdk)
- **Northrays API**: [RESTful API](/docs/en/tools/api#northrays) ([OpenAPI spec](/docs/openapi.json)), [Toolbox API](/docs/en/tools/api#northrays-toolbox) ([OpenAPI spec](/docs/toolbox-openapi.json))
- **Northrays CLI**: [Mac/Linux](/docs/en/getting-started#cli), [Windows](/docs/en/getting-started#cli), [Reference](/docs/en/tools/cli)

:::tip
For faster development with AI agents and assistants, use our LLMs context files: [llms.txt](https://www.northrays.com/docs/llms.txt) and [llms-full.txt](https://www.northrays.com/docs/llms-full.txt), [agent skills](https://github.com/northrays/skills), and markdown pages by appending `.md` to URLs.

&nbsp;

```text
npx skills add https://github.com/northrays/skills --skill northrays
```
:::

## 1. Create an account

Open the [Northrays Dashboard ↗](https://app.northrays.com/) to create your account. Northrays supports account creation using an email and password, or by connecting your Google or GitHub account.

## 2. Obtain an API key

Generate an API key from the [Northrays Dashboard ↗](https://app.northrays.com/dashboard/keys) or using the [Northrays API](/docs/en/tools/api/#northrays/tag/api-keys/POST/api-keys) to authenticate SDK requests and access Northrays services. Northrays supports multiple options to configure your environment and API keys: [in code](/docs/en/configuration#configuration-in-code), [environment variables](/docs/en/configuration#environment-variables), [.env file](/docs/en/configuration#env-file), and [default values](/docs/en/configuration#default-values).

## 3. Install the SDK

Install the Northrays **Python**, **TypeScript**, **Ruby**, **Go**, or **Java** SDKs to interact with sandboxes.

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```bash
pip install northrays
```

</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">

```bash
npm install @northrays/sdk
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">

```bash
gem install northrays
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```bash
go get github.com/northrays/sandbox-platform/libs/sdk-go
```

</TabItem>
<TabItem label="Java" icon="seti:java">

**Gradle**

Add the Northrays SDK dependency to your `build.gradle.kts`:

```kotlin
dependencies {
    implementation("io.northrays:sdk:x.y.z")
}
```

**Maven**

Add the Northrays SDK dependency to your `pom.xml`:

```xml
<dependency>
  <groupId>io.northrays</groupId>
  <artifactId>sdk</artifactId>
  <version>x.y.z</version>
</dependency>
```

</TabItem>
</Tabs>

## 4. Create a Sandbox

Create a sandbox to run your code securely in an isolated environment.

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```python
# Import the Northrays SDK
from northrays import Northrays, NorthraysConfig

# Define the configuration
config = NorthraysConfig(api_key="YOUR_API_KEY") # Replace with your API key

# Initialize the Northrays client
northrays = Northrays(config)

# Create the Sandbox instance
sandbox = northrays.create()
```

</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">

```typescript
// Import the Northrays SDK
import { Northrays } from '@northrays/sdk'

// Initialize the Northrays client
const northrays = new Northrays({ apiKey: 'YOUR_API_KEY' }) // Replace with your API key

// Create the Sandbox instance
const sandbox = await northrays.create()
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">

```ruby
require 'northrays'

# Initialize the Northrays client
config = Northrays::Config.new(api_key: 'YOUR_API_KEY') # Replace with your API key

# Create the Northrays client
northrays = Northrays::Northrays.new(config)

# Create the Sandbox instance
sandbox = northrays.create
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```go
package main

import (
	"context"
	"fmt"

	"github.com/northrays/sandbox-platform/libs/sdk-go/pkg/northrays"
	"github.com/northrays/sandbox-platform/libs/sdk-go/pkg/types"
)

func main() {
	config := &types.NorthraysConfig{
        APIKey: "YOUR_API_KEY", // Replace with your API key
    }
	client, _ := northrays.NewClientWithConfig(config)
	ctx := context.Background()
	sandbox, _ := client.Create(ctx, nil)
	fmt.Println(sandbox.ID)
}
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.northrays.sdk.Northrays;
import io.northrays.sdk.Sandbox;
import io.northrays.sdk.NorthraysConfig;

public class Main {
    public static void main(String[] args) {
        NorthraysConfig config = new NorthraysConfig.Builder()
            .apiKey("YOUR_API_KEY")
            .build();

        try (Northrays northrays = new Northrays(config)) {
            Sandbox sandbox = northrays.create();
            System.out.println(sandbox.getId());
        }
    }
}
```

</TabItem>
<TabItem label="API" icon="seti:json">

```bash
curl https://app.northrays.com/api/sandbox \
  --request POST \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer YOUR_API_KEY' \
  --data '{}'
```

</TabItem>
<TabItem label="CLI" icon="seti:shell">

```shell
northrays create
```

</TabItem>
</Tabs>

## 5. Write and run code

Create a program that runs code inside a sandbox. The following snippets are examples of "Hello World" programs that run securely inside a sandbox.

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```python
# Import the Northrays SDK
from northrays import Northrays, NorthraysConfig

# Define the configuration
config = NorthraysConfig(api_key="YOUR_API_KEY") # Replace with your API key

# Initialize the Northrays client
northrays = Northrays(config)

# Create the Sandbox instance
sandbox = northrays.create()

# Run the code securely inside the Sandbox
response = sandbox.process.code_run('print("Hello World")')

# Check the response
if response.exit_code != 0:
  print(f"Error: {response.exit_code} {response.result}")
else:
  print(response.result)

# Clean up
sandbox.delete()
```

</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">

```typescript
// Import the Northrays SDK
import { Northrays } from '@northrays/sdk'

// Initialize the Northrays client
const northrays = new Northrays({ apiKey: 'YOUR_API_KEY' }) // Replace with your API key

// Create the Sandbox instance
const sandbox = await northrays.create({
  language: 'typescript',
})

// Run the code securely inside the Sandbox
const response = await sandbox.process.codeRun('console.log("Hello World")')

// Check the response
if (response.exitCode !== 0) {
  console.error(`Error: ${response.exitCode} ${response.result}`)
} else {
  console.log(response.result)
}

// Clean up
await sandbox.delete()
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">

```ruby
require 'northrays'

# Initialize the Northrays client
config = Northrays::Config.new(api_key: 'YOUR_API_KEY')
northrays = Northrays::Northrays.new(config)

# Create the Sandbox instance
sandbox = northrays.create

# Run the code securely inside the Sandbox
response = sandbox.process.code_run(code: 'print("Hello World")')
puts response.result
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```go
// Import the Northrays SDK
package main

import (
    "context"
    "log"

    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/northrays"
    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/types"
)

func main() {
    // Define the configuration
    config := &types.NorthraysConfig{
        APIKey: "YOUR_API_KEY", // Replace with your API key
    }

    // Initialize the Northrays client
    client, err := northrays.NewClientWithConfig(config)
    if err != nil {
        log.Fatal(err)
    }

    ctx := context.Background()

    // Create the Sandbox instance
    params := types.SnapshotParams{
        SandboxBaseParams: types.SandboxBaseParams{
            Language: types.CodeLanguagePython,
        },
    }
    sandbox, err := client.Create(ctx, params)
    if err != nil {
        log.Fatal(err)
    }

    // Run the code securely inside the Sandbox
    result, err := sandbox.Process.ExecuteCommand(ctx, `echo "Hello World"`)

    // Check the response
    if err != nil {
        log.Fatalf("Error: %v", err)
    }
    if result.ExitCode != 0 {
        log.Printf("Error: %d %s", result.ExitCode, result.Result)
    } else {
        log.Println(result.Result)
    }

    // Clean up
    sandbox.Delete(ctx)
}
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.northrays.sdk.Northrays;
import io.northrays.sdk.NorthraysConfig;
import io.northrays.sdk.Sandbox;
import io.northrays.sdk.model.ExecuteResponse;

public class Main {
    public static void main(String[] args) {
        NorthraysConfig config = new NorthraysConfig.Builder()
            .apiKey("YOUR_API_KEY") // Replace with your API key
            .build();

        try (Northrays northrays = new Northrays(config)) {
            // Create the Sandbox instance
            Sandbox sandbox = northrays.create();

            // Run the code securely inside the Sandbox
            ExecuteResponse response = sandbox.getProcess().executeCommand("echo \"Hello World\"");

            // Check the response
            if (response.getExitCode() != 0) {
                System.err.println("Error: " + response.getExitCode() + " " + response.getResult());
            } else {
                System.out.println(response.getResult());
            }

            // Clean up
            sandbox.delete();
        }
    }
}
```

</TabItem>
</Tabs>

## Summary

By following the steps above, you successfully create a Northrays account, obtain an API key, install the SDK, create a sandbox, write code, and run it securely in a sandbox.

## Next steps

Use the following resources to interact with sandboxes:

- Learn more about Northrays with the [getting started](/docs/en/getting-started) and [sandboxes](/docs/en/sandboxes) guides
- View [examples](/docs/en/getting-started#examples) for common sandbox operations and best practices
- Explore [guides](/docs/en/guides) to connect Northrays with [Claude](/docs/en/guides/claude), [OpenCode](/docs/en/guides/opencode/opencode-web-agent), [Codex](/docs/en/guides/codex/codex-sdk-interactive-terminal-sandbox), [LangChain](/docs/en/guides/langchain/langchain-data-analysis) and more

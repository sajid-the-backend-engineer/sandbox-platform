---
title: "Northrays"
hideTitleOnPage: true
---

## Northrays

Northrays class for Northrays SDK.

### Constructors

#### new Northrays()

```ruby
def initialize(config)

```

**Parameters**:

- `config` _Northrays:Config_ - Configuration options. Defaults to Northrays::Config.new

**Returns**:

- `Northrays` - a new instance of Northrays

### Methods

#### config()

```ruby
def config()

```

**Returns**:

- `Northrays:Config`

#### api_client()

```ruby
def api_client()

```

**Returns**:

- `NorthraysApiClient`

#### sandbox_api()

```ruby
def sandbox_api()

```

**Returns**:

- `NorthraysApiClient:SandboxApi`

#### volume()

```ruby
def volume()

```

**Returns**:

- `Northrays:VolumeService`

#### object_storage_api()

```ruby
def object_storage_api()

```

**Returns**:

- `NorthraysApiClient:ObjectStorageApi`

#### snapshots_api()

```ruby
def snapshots_api()

```

**Returns**:

- `NorthraysApiClient:SnapshotsApi`

#### snapshot()

```ruby
def snapshot()

```

**Returns**:

- `Northrays:SnapshotService`

#### close()

```ruby
def close()

```

Shuts down OTel providers, flushing any pending telemetry data.

**Returns**:

- `void`

#### create()

```ruby
def create(params, on_snapshot_create_logs:)

```

Creates a sandbox with the specified parameters

**Parameters**:

- `params` _Northrays:CreateSandboxFromSnapshotParams, Northrays:CreateSandboxFromImageParams, Nil_ - Sandbox creation parameters

**Returns**:

- `Northrays:Sandbox` - The created sandbox

**Raises**:

- `Northrays:Sdk:Error` - If auto_stop_interval or auto_archive_interval is negative

#### delete()

```ruby
def delete(sandbox)

```

Deletes a Sandbox.

**Parameters**:

- `sandbox` _Northrays:Sandbox_ -

**Returns**:

- `void`

#### get()

```ruby
def get(id)

```

Gets a Sandbox by its ID.

**Parameters**:

- `id` _String_ -

**Returns**:

- `Northrays:Sandbox`

#### list()

```ruby
def list(query)

```

Iterates over Sandboxes matching the given query.

**Parameters**:

- `query` _Northrays:ListSandboxesQuery, nil_ - Optional filters, sorting, and per-page size.

**Returns**:

- `Enumerator\<Northrays:Sandbox\>`

**Raises**:

- `Northrays:Sdk:Error` -

**Examples:**

```ruby
northrays.list(Northrays::ListSandboxesQuery.new(labels: { 'env' => 'dev' })).each do |sandbox|
  puts sandbox.id
end

```

#### start()

```ruby
def start(sandbox, timeout)

```

Starts a Sandbox and waits for it to be ready.

**Parameters**:

- `sandbox` _Northrays:Sandbox_ -
- `timeout` _Numeric_ - Maximum wait time in seconds (defaults to 60 s).

**Returns**:

- `void`

#### stop()

```ruby
def stop(sandbox, timeout)

```

Stops a Sandbox and waits for it to be stopped.

**Parameters**:

- `sandbox` _Northrays:Sandbox_ -
- `timeout` _Numeric_ - Maximum wait time in seconds (defaults to 60 s).

**Returns**:

- `void`

---
title: "Config"
hideTitleOnPage: true
---

## Config

Main class for a new Northrays::Config object.

### Constructors

#### new Config()

```ruby
def initialize(api_key:, jwt_token:, api_url:, organization_id:, target:, otel_enabled:, _experimental:)

```

Initializes a new Northrays::Config object.

**Parameters**:

- `api_key` _String, nil_ - Northrays API key. Defaults to ENV['NORTHRAYS_API_KEY'].
- `jwt_token` _String, nil_ - Northrays JWT token. Defaults to ENV['NORTHRAYS_JWT_TOKEN'].
- `api_url` _String, nil_ - Northrays API URL. Defaults to ENV['NORTHRAYS_API_URL'] or Northrays::Config::API_URL.
- `organization_id` _String, nil_ - Northrays organization ID. Defaults to ENV['NORTHRAYS_ORGANIZATION_ID'].
- `target` _String, nil_ - Northrays target. Defaults to ENV['NORTHRAYS_TARGET'].
- `otel_enabled` _Boolean, nil_ - Enable OpenTelemetry tracing for SDK operations.
- `_experimental` _Hash, nil_ - Experimental configuration options.

**Returns**:

- `Config` - a new instance of Config

### Methods

#### api_key()

```ruby
def api_key()

```

API key for authentication with the Northrays API

**Returns**:

- `String, nil` - Northrays API key

#### api_key=()

```ruby
def api_key=(value)

```

API key for authentication with the Northrays API

**Returns**:

- `String, nil` - Northrays API key

#### jwt_token()

```ruby
def jwt_token()

```

JWT token for authentication with the Northrays API

**Returns**:

- `String, nil` - Northrays JWT token

#### jwt_token=()

```ruby
def jwt_token=(value)

```

JWT token for authentication with the Northrays API

**Returns**:

- `String, nil` - Northrays JWT token

#### api_url()

```ruby
def api_url()

```

URL of the Northrays API

**Returns**:

- `String, nil` - Northrays API URL

#### api_url=()

```ruby
def api_url=(value)

```

URL of the Northrays API

**Returns**:

- `String, nil` - Northrays API URL

#### organization_id()

```ruby
def organization_id()

```

Organization ID for authentication with the Northrays API

**Returns**:

- `String, nil` - Northrays API URL

#### organization_id=()

```ruby
def organization_id=(value)

```

Organization ID for authentication with the Northrays API

**Returns**:

- `String, nil` - Northrays API URL

#### target()

```ruby
def target()

```

Target environment for sandboxes

**Returns**:

- `String, nil` - Northrays target

#### target=()

```ruby
def target=(value)

```

Target environment for sandboxes

**Returns**:

- `String, nil` - Northrays target

#### otel_enabled()

```ruby
def otel_enabled()

```

Enable OpenTelemetry tracing for SDK operations.

**Returns**:

- `Boolean, nil`

#### otel_enabled=()

```ruby
def otel_enabled=(value)

```

Enable OpenTelemetry tracing for SDK operations.

**Returns**:

- `Boolean, nil`

#### _experimental()

```ruby
def _experimental()

```

Experimental configuration options

**Returns**:

- `Hash, nil` - Experimental configuration hash

#### _experimental=()

```ruby
def _experimental=(value)

```

Experimental configuration options

**Returns**:

- `Hash, nil` - Experimental configuration hash

#### read_env()

```ruby
def read_env(name)

```

Reads a NORTHRAYS_-prefixed environment variable using the same precedence
as the Config initializer: runtime ENV first, then .env.local, then .env.
Only names starting with NORTHRAYS_ are accepted.

**Parameters**:

- `name` _String_ - The environment variable name. Must start with NORTHRAYS_.

**Returns**:

- `String, nil` - The value of the environment variable, or nil if not set.

**Raises**:

- `ArgumentError` - If name does not start with NORTHRAYS_.

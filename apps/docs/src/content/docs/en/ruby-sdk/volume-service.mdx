---
title: "VolumeService"
hideTitleOnPage: true
---

## VolumeService

Service for managing Northrays Volumes. Can be used to list, get, create and delete Volumes.

### Constructors

#### new VolumeService()

```ruby
def initialize(volumes_api, otel_state:)

```

Service for managing Northrays Volumes. Can be used to list, get, create and delete Volumes.

**Parameters**:

- `volumes_api` _NorthraysApiClient:VolumesApi_ -
- `otel_state` _Northrays:OtelState, nil_ -

**Returns**:

- `VolumeService` - a new instance of VolumeService

### Methods

#### create()

```ruby
def create(name)

```

Create new Volume.

**Parameters**:

- `name` _String_ -

**Returns**:

- `Northrays:Volume`

#### delete()

```ruby
def delete(volume)

```

Delete a Volume.

**Parameters**:

- `volume` _Northrays:Volume_ -

**Returns**:

- `void`

#### get()

```ruby
def get(name, create:)

```

Get a Volume by name.

**Parameters**:

- `name` _String_ -
- `create` _Boolean_ -

**Returns**:

- `Northrays:Volume`

#### list()

```ruby
def list()

```

List all Volumes.

**Returns**:

- `Array\<Northrays:Volume\>`

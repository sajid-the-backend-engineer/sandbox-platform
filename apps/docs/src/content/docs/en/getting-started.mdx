---
title: Getting Started
description: Get started with Northrays and learn how to create your first sandbox.
---

import { TabItem, Tabs } from '@astrojs/starlight/components'

This section introduces core concepts, common workflows, and next steps for using Northrays.

## Dashboard

[Northrays Dashboard ↗](https://app.northrays.com/) is a visual user interface where you can manage sandboxes, access API keys, view usage, and more.
It serves as the primary point of control for managing your Northrays resources.

## SDKs

Northrays provides [Python](/docs/en/python-sdk), [TypeScript](/docs/en/typescript-sdk), [Ruby](/docs/en/ruby-sdk), [Go](/docs/en/go-sdk), and [Java](/docs/en/java-sdk) SDKs to programmatically interact with sandboxes. They support sandbox lifecycle management, code execution, resource access, and more.

## CLI

Northrays provides command-line access to core features for interacting with Northrays Sandboxes, including managing their lifecycle, snapshots, and more. To interact with Northrays Sandboxes from the command line, install the Northrays CLI:

<Tabs syncKey="language">
<TabItem label="Mac">

```bash
brew install northrays/cli/northrays
```

Trust the tap once so routine `brew upgrade` keeps the Northrays CLI up to date. Recent Homebrew versions require third-party taps to be explicitly trusted; without it, a bare `brew upgrade` skips the Northrays tap and the CLI goes stale:

```bash
brew trust northrays/cli
```

To upgrade the Northrays CLI to the latest version:

```bash
brew upgrade northrays/cli/northrays
```

Alternatively, install directly without Homebrew:

For Apple Silicon (`arm64`):

  ```bash
  sudo curl -fL https://github.com/northrays/sandbox-platform/releases/latest/download/northrays-darwin-arm64 -o /usr/local/bin/northrays && sudo chmod +x /usr/local/bin/northrays
  ```

For Intel (`amd64`):

  ```bash
  sudo curl -fL https://github.com/northrays/sandbox-platform/releases/latest/download/northrays-darwin-amd64 -o /usr/local/bin/northrays && sudo chmod +x /usr/local/bin/northrays
  ```

</TabItem>
<TabItem label="Linux">

Choose the command for your Linux architecture. Both commands download the latest binary from GitHub releases and install it to `/usr/local/bin`, overwriting any existing version.

For `amd64` (`x86_64`):

  ```bash
  sudo curl -fL https://github.com/northrays/sandbox-platform/releases/latest/download/northrays-linux-amd64 -o /usr/local/bin/northrays && sudo chmod +x /usr/local/bin/northrays
  ```

For `arm64` (`aarch64`):

  ```bash
  sudo curl -fL https://github.com/northrays/sandbox-platform/releases/latest/download/northrays-linux-arm64 -o /usr/local/bin/northrays && sudo chmod +x /usr/local/bin/northrays
  ```

</TabItem>
<TabItem label="Windows">

```bash
powershell -Command "irm https://get.northrays.com/windows | iex"
```

</TabItem>
</Tabs>

After installing the Northrays CLI, use the `northrays` command to interact with Northrays sandboxes from the command line.

To view all available commands and flags, see the [CLI reference](/docs/en/tools/cli).

## API

Northrays provides a RESTful API for interacting with Northrays Sandboxes, including managing their lifecycle, snapshots, and more. 
It serves as a flexible and powerful way to interact with Northrays from your own applications.

To interact with Northrays Sandboxes from the API, see the [API reference](/docs/en/tools/api).

## MCP server

Northrays provides a Model Context Protocol (MCP) server that enables AI agents to interact with Northrays Sandboxes programmatically. The MCP server integrates with popular AI agents including Claude, Cursor, and Windsurf.

To set up the MCP server with your AI agent:

```bash
northrays mcp init [claude/cursor/windsurf]
```

For more information, see the [MCP server documentation](/docs/en/mcp).

## Multiple runtime support

The [TypeScript SDK](/docs/en/typescript-sdk) ships as a dual ESM/CJS package and works out of the box in **Node.js**, **Bun**, **Next.js**, **Nuxt.js**, **Remix**, **Vite SSR**, **AWS Lambda**, and **Azure Functions** without any extra configuration.

For **Cloudflare Workers**, set the Node.js compatibility flag in your `wrangler.toml`:

```toml
compatibility_flags = ["nodejs_compat"]
```

For **Deno**, install with `deno add npm:@northrays/sdk` or import directly with the `npm:` specifier:

```typescript
import { Northrays, Image } from 'npm:@northrays/sdk'
```

For **browser apps with Vite** (or any browser bundler), install [`vite-plugin-node-polyfills`](https://www.npmjs.com/package/vite-plugin-node-polyfills) and add it to your `vite.config.ts`:

```typescript
import { defineConfig } from 'vite'
import { nodePolyfills } from 'vite-plugin-node-polyfills'

export default defineConfig({
  plugins: [nodePolyfills({ globals: { Buffer: true, process: true, global: true } })],
})
```

The SDK uses Node's `Buffer` for binary data (downloaded files, multipart bodies). Browsers don't ship `Buffer`, so the polyfill provides it. Without it, basic operations like `Image.base()` and `northrays.list()` still work, but methods that handle binary payloads (`fs.downloadFile`, `fs.downloadFiles`) will throw.

Some runtimes don't expose the full set of Node.js APIs (browsers and edge runtimes have no filesystem, no `crypto`, etc.). Methods that depend on those APIs throw a clear runtime error instead of silently producing wrong results.

## Guides

Northrays provides a comprehensive set of [guides](/docs/en/guides) to help you get started. The guides cover a wide range of topics, from basic usage to advanced topics, and showcase various types of integrations between Northrays and other tools.

## Examples

Northrays provides quick examples for common sandbox operations and best practices. <br />
The examples are based on the Northrays [Python](/docs/en/python-sdk/), [TypeScript](/docs/en/typescript-sdk/), [Go](/docs/en/go-sdk/), [Ruby](/docs/en/ruby-sdk/), [Java](/docs/en/java-sdk/) **SDKs**, [CLI](/docs/en/tools/cli), and [API](/docs/en/tools/api) references. More examples are available in our [GitHub repository](https://github.com/northrays/sandbox-platform/tree/main/examples).

### Create a sandbox

Create a [sandbox](/docs/en/sandboxes) with default settings.

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```python
from northrays import Northrays

northrays = Northrays()
sandbox = northrays.create()
print(f"Sandbox ID: {sandbox.id}")
```

</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">

```typescript
import { Northrays } from '@northrays/sdk';

const northrays = new Northrays();
const sandbox = await northrays.create();
console.log(`Sandbox ID: ${sandbox.id}`);
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```go
package main

import (
    "context"
    "fmt"
    "log"

    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/northrays"
)

func main() {
    client, err := northrays.NewClient()
    if err != nil {
        log.Fatal(err)
    }

    sandbox, err := client.Create(context.Background(), nil)
    if err != nil {
        log.Fatal(err)
    }
    fmt.Printf("Sandbox ID: %s\n", sandbox.ID)
}
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">

```ruby
require 'northrays'

northrays = Northrays::Northrays.new
sandbox = northrays.create
puts "Sandbox ID: #{sandbox.id}"
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.northrays.sdk.Northrays;
import io.northrays.sdk.Sandbox;

public class App {
    public static void main(String[] args) {
        try (Northrays northrays = new Northrays()) {
            Sandbox sandbox = northrays.create();
            System.out.println("Sandbox ID: " + sandbox.getId());
        }
    }
}
```

</TabItem>
<TabItem label="CLI" icon="seti:shell">

```shell
northrays create
```

</TabItem>
<TabItem label="API" icon="seti:json">

```bash
curl 'https://app.northrays.com/api/sandbox' \
  --request POST \
  --header 'Authorization: Bearer <API_KEY>' \
  --header 'Content-Type: application/json' \
  --data '{}'
```

</TabItem>
</Tabs>

### Create and run code in a sandbox

Create a [sandbox](/docs/en/sandboxes) and run code securely in it.

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```python
from northrays import Northrays

northrays = Northrays()
sandbox = northrays.create()
response = sandbox.process.exec("echo 'Hello, World!'")
print(response.result)
sandbox.delete()
```

</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">
```typescript
import { Northrays } from '@northrays/sdk';

const northrays = new Northrays();
const sandbox = await northrays.create();
const response = await sandbox.process.executeCommand('echo "Hello, World!"');
console.log(response.result);
await sandbox.delete();
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```go
package main

import (
    "context"
    "fmt"
    "log"

    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/northrays"
)

func main() {
    client, err := northrays.NewClient()
    if err != nil {
        log.Fatal(err)
    }

    sandbox, err := client.Create(context.Background(), nil)
    if err != nil {
        log.Fatal(err)
    }

    response, err := sandbox.Process.ExecuteCommand(context.Background(), "echo 'Hello, World!'")
    if err != nil {
        log.Fatal(err)
    }
    fmt.Println(response.Result)
    sandbox.Delete(context.Background())
}
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">

```ruby
require 'northrays'

northrays = Northrays::Northrays.new
sandbox = northrays.create
response = sandbox.process.exec(command: "echo 'Hello, World!'")
puts response.result
northrays.delete(sandbox)
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.northrays.sdk.Northrays;
import io.northrays.sdk.Sandbox;
import io.northrays.sdk.model.ExecuteResponse;

public class App {
    public static void main(String[] args) {
        try (Northrays northrays = new Northrays()) {
            Sandbox sandbox = northrays.create();
            ExecuteResponse response = sandbox.process.executeCommand("echo 'Hello, World!'");
            System.out.println(response.getResult());
            sandbox.delete();
        }
    }
}
```

</TabItem>
<TabItem label="CLI" icon="seti:shell">

```shell
northrays create --name my-sandbox
northrays exec my-sandbox -- echo 'Hello, World!'
northrays delete my-sandbox
```

</TabItem>
<TabItem label="API" icon="seti:json">

```bash
# Create a sandbox
curl 'https://app.northrays.com/api/sandbox' \
  --request POST \
  --header 'Authorization: Bearer <API_KEY>' \
  --header 'Content-Type: application/json' \
  --data '{}'

# Execute a command in the sandbox
curl 'https://proxy.app.northrays.com/toolbox/{sandboxId}/process/execute' \
  --request POST \
  --header 'Authorization: Bearer <API_KEY>' \
  --header 'Content-Type: application/json' \
  --data '{
  "command": "echo '\''Hello, World!'\''"
}'

# Delete the sandbox
curl 'https://app.northrays.com/api/sandbox/{sandboxId}' \
  --request DELETE \
  --header 'Authorization: Bearer <API_KEY>'
```

</TabItem>
</Tabs>

### Create a sandbox with custom resources

Create a sandbox with [custom resources](/docs/en/sandboxes#resources) (CPU, memory, disk).

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```python
from northrays import Northrays, CreateSandboxFromImageParams, Image, Resources

northrays = Northrays()
sandbox = northrays.create(
    CreateSandboxFromImageParams(
        image=Image.debian_slim("3.12"),
        resources=Resources(cpu=2, memory=4, disk=8)
    )
)
```

</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">

```typescript
import { Northrays, Image } from '@northrays/sdk';

const northrays = new Northrays();
const sandbox = await northrays.create({
    image: Image.debianSlim('3.12'),
    resources: { cpu: 2, memory: 4, disk: 8 }
});
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```go
package main

import (
    "context"
    "log"

    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/northrays"
    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/types"
)

func main() {
    client, err := northrays.NewClient()
    if err != nil {
        log.Fatal(err)
    }

    sandbox, err := client.Create(context.Background(), types.ImageParams{
        Image: northrays.DebianSlim(nil),
        Resources: &types.Resources{
            CPU:    2,
            Memory: 4,
            Disk:   8,
        },
    })
    if err != nil {
        log.Fatal(err)
    }
}
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">

```ruby
require 'northrays'

northrays = Northrays::Northrays.new
sandbox = northrays.create(
    Northrays::CreateSandboxFromImageParams.new(
        image: Northrays::Image.debian_slim('3.12'),
        resources: Northrays::Resources.new(cpu: 2, memory: 4, disk: 8)
    )
)
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.northrays.sdk.Northrays;
import io.northrays.sdk.Image;
import io.northrays.sdk.Sandbox;
import io.northrays.sdk.model.CreateSandboxFromImageParams;
import io.northrays.sdk.model.Resources;

public class App {
    public static void main(String[] args) {
        try (Northrays northrays = new Northrays()) {
            CreateSandboxFromImageParams params = new CreateSandboxFromImageParams();
            params.setImage(Image.debianSlim("3.12"));
            Resources resources = new Resources();
            resources.setCpu(2);
            resources.setMemory(4);
            resources.setDisk(8);
            params.setResources(resources);
            Sandbox sandbox = northrays.create(params);
        }
    }
}
```

</TabItem>
<TabItem label="CLI" icon="seti:shell">

```shell
northrays create --class small
```

</TabItem>
<TabItem label="API" icon="seti:json">

```bash
curl 'https://app.northrays.com/api/sandbox' \
  --request POST \
  --header 'Authorization: Bearer <API_KEY>' \
  --header 'Content-Type: application/json' \
  --data '{
  "cpu": 2,
  "memory": 4,
  "disk": 8
}'
```

</TabItem>
</Tabs>

### Create an ephemeral sandbox

Create an [ephemeral sandbox](/docs/en/sandboxes#ephemeral-sandboxes) that is automatically deleted when stopped.

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```python
from northrays import Northrays, CreateSandboxFromSnapshotParams

northrays = Northrays()
sandbox = northrays.create(
    CreateSandboxFromSnapshotParams(ephemeral=True, auto_stop_interval=5)
)
```

</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">

```typescript
import { Northrays } from '@northrays/sdk';

const northrays = new Northrays();
const sandbox = await northrays.create({
    ephemeral: true,
    autoStopInterval: 5
});
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```go
package main

import (
    "context"
    "log"

    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/northrays"
    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/types"
)

func main() {
    client, err := northrays.NewClient()
    if err != nil {
        log.Fatal(err)
    }

    autoStop := 5
    sandbox, err := client.Create(context.Background(), types.SnapshotParams{
        SandboxBaseParams: types.SandboxBaseParams{
            Ephemeral:        true,
            AutoStopInterval: &autoStop,
        },
    })
    if err != nil {
        log.Fatal(err)
    }
}
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">

```ruby
require 'northrays'

northrays = Northrays::Northrays.new
sandbox = northrays.create(
    Northrays::CreateSandboxFromSnapshotParams.new(ephemeral: true, auto_stop_interval: 5)
)
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.northrays.sdk.Northrays;
import io.northrays.sdk.Sandbox;
import io.northrays.sdk.model.CreateSandboxFromSnapshotParams;

public class App {
    public static void main(String[] args) {
        try (Northrays northrays = new Northrays()) {
            CreateSandboxFromSnapshotParams params = new CreateSandboxFromSnapshotParams();
            params.setAutoDeleteInterval(0); // same effect as ephemeral: true
            params.setAutoStopInterval(5);
            Sandbox sandbox = northrays.create(params);
        }
    }
}
```

</TabItem>

<TabItem label="CLI" icon="seti:shell">

```shell
northrays create --auto-stop 5 --auto-delete 0
```

</TabItem>
<TabItem label="API" icon="seti:json">

```bash
curl 'https://app.northrays.com/api/sandbox' \
  --request POST \
  --header 'Authorization: Bearer <API_KEY>' \
  --header 'Content-Type: application/json' \
  --data '{
  "autoStopInterval": 5,
  "autoDeleteInterval": 0
}'
```

</TabItem>
</Tabs>

### Create a sandbox from a snapshot

Create a sandbox from a pre-built [snapshot](/docs/en/snapshots) for faster sandbox creation with pre-installed dependencies.

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">
```python
from northrays import Northrays, CreateSandboxFromSnapshotParams

northrays = Northrays()
sandbox = northrays.create(
    CreateSandboxFromSnapshotParams(
        snapshot="my-snapshot-name",
        language="python"
    )
)
```

</TabItem>

<TabItem label="TypeScript" icon="seti:typescript">
```typescript
import { Northrays } from '@northrays/sdk';

const northrays = new Northrays();
const sandbox = await northrays.create({
    snapshot: 'my-snapshot-name',
    language: 'typescript'
});
```

</TabItem>

<TabItem label="Go" icon="seti:go">
```go
package main

import (
    "context"
    "log"

    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/northrays"
    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/types"
)

func main() {
    client, err := northrays.NewClient()
    if err != nil {
        log.Fatal(err)
    }

    sandbox, err := client.Create(context.Background(), types.SnapshotParams{
        Snapshot: "my-snapshot-name",
        SandboxBaseParams: types.SandboxBaseParams{
            Language: types.CodeLanguagePython,
        },
    })
    if err != nil {
        log.Fatal(err)
    }
}
```

</TabItem>

<TabItem label="Ruby" icon="seti:ruby">
```ruby
require 'northrays'

northrays = Northrays::Northrays.new
sandbox = northrays.create(
    Northrays::CreateSandboxFromSnapshotParams.new(
        snapshot: 'my-snapshot-name',
        language: Northrays::CodeLanguage::PYTHON
    )
)
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.northrays.sdk.Northrays;
import io.northrays.sdk.Sandbox;
import io.northrays.sdk.model.CreateSandboxFromSnapshotParams;

public class App {
    public static void main(String[] args) {
        try (Northrays northrays = new Northrays()) {
            CreateSandboxFromSnapshotParams params = new CreateSandboxFromSnapshotParams();
            params.setSnapshot("my-snapshot-name");
            params.setLanguage("python");
            Sandbox sandbox = northrays.create(params);
        }
    }
}
```

</TabItem>

<TabItem label="CLI" icon="seti:shell">
```shell
northrays create --snapshot my-snapshot-name
```

</TabItem>
<TabItem label="API" icon="seti:json">

```bash
curl 'https://app.northrays.com/api/sandbox' \
  --request POST \
  --header 'Authorization: Bearer <API_KEY>' \
  --header 'Content-Type: application/json' \
  --data '{
  "snapshot": "my-snapshot-name"
}'
```

</TabItem>
</Tabs>

### Create a sandbox with a declarative image

Create a sandbox with a [declarative image](/docs/en/declarative-builder) that defines dependencies programmatically.

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```python
from northrays import Northrays, CreateSandboxFromImageParams, Image

northrays = Northrays()
image = (
    Image.debian_slim("3.12")
    .pip_install(["requests", "pandas", "numpy"])
    .workdir("/home/northrays")
)
sandbox = northrays.create(
    CreateSandboxFromImageParams(image=image),
    on_snapshot_create_logs=print
)
```

</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">

```typescript
import { Northrays, Image } from '@northrays/sdk';

const northrays = new Northrays();
const image = Image.debianSlim('3.12')
    .pipInstall(['requests', 'pandas', 'numpy'])
    .workdir('/home/northrays');
const sandbox = await northrays.create(
    { image },
    { onSnapshotCreateLogs: console.log }
);
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```go
package main

import (
    "context"
    "log"

    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/northrays"
    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/types"
)

func main() {
    client, err := northrays.NewClient()
    if err != nil {
        log.Fatal(err)
    }

    image := northrays.DebianSlim(nil).
        PipInstall([]string{"requests", "pandas", "numpy"}).
        Workdir("/home/northrays")
    sandbox, err := client.Create(context.Background(), types.ImageParams{
        Image: image,
    })
    if err != nil {
        log.Fatal(err)
    }
}
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">

```ruby
require 'northrays'

northrays = Northrays::Northrays.new
image = Northrays::Image
    .debian_slim('3.12')
    .pip_install(['requests', 'pandas', 'numpy'])
    .workdir('/home/northrays')
sandbox = northrays.create(
    Northrays::CreateSandboxFromImageParams.new(image: image),
    on_snapshot_create_logs: proc { |chunk| puts chunk }
)
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.northrays.sdk.Northrays;
import io.northrays.sdk.Image;
import io.northrays.sdk.Sandbox;
import io.northrays.sdk.model.CreateSandboxFromImageParams;

public class App {
    public static void main(String[] args) {
        try (Northrays northrays = new Northrays()) {
            Image image = Image.debianSlim("3.12")
                    .pipInstall("requests", "pandas", "numpy")
                    .workdir("/home/northrays");
            CreateSandboxFromImageParams params = new CreateSandboxFromImageParams();
            params.setImage(image);
            Sandbox sandbox = northrays.create(params, 60, System.out::println);
        }
    }
}
```

</TabItem>
<TabItem label="CLI" icon="seti:shell">

```shell
northrays create --dockerfile ./Dockerfile
```

</TabItem>
<TabItem label="API" icon="seti:json">

```bash
curl 'https://app.northrays.com/api/sandbox' \
  --request POST \
  --header 'Authorization: Bearer <API_KEY>' \
  --header 'Content-Type: application/json' \
  --data '{
  "buildInfo": {
    "dockerfileContent": "FROM python:3.12-slim\nRUN pip install requests pandas numpy\nWORKDIR /home/northrays"
  }
}'
```

</TabItem>
</Tabs>

### Create a sandbox with volumes

Create a sandbox with a [volume](/docs/en/volumes) mounted to share data across sandboxes.

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```python
from northrays import Northrays, CreateSandboxFromSnapshotParams, VolumeMount

northrays = Northrays()
volume = northrays.volume.get("my-volume", create=True)
sandbox = northrays.create(
    CreateSandboxFromSnapshotParams(
        volumes=[VolumeMount(volume_id=volume.id, mount_path="/home/northrays/data")]
    )
)
```

</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">

```typescript
import { Northrays } from '@northrays/sdk';

const northrays = new Northrays();
const volume = await northrays.volume.get('my-volume', true);
const sandbox = await northrays.create({
    volumes: [{ volumeId: volume.id, mountPath: '/home/northrays/data' }]
});
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```go
package main

import (
    "context"
    "log"

    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/northrays"
    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/types"
)

func main() {
    client, err := northrays.NewClient()
    if err != nil {
        log.Fatal(err)
    }

    volume, err := client.Volume.Get(context.Background(), "my-volume")
    if err != nil {
        volume, err = client.Volume.Create(context.Background(), "my-volume")
        if err != nil {
            log.Fatal(err)
        }
    }

    sandbox, err := client.Create(context.Background(), types.SnapshotParams{
        SandboxBaseParams: types.SandboxBaseParams{
            Volumes: []types.VolumeMount{{
                VolumeID:  volume.ID,
                MountPath: "/home/northrays/data",
            }},
        },
    })
    if err != nil {
        log.Fatal(err)
    }
}
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">
```ruby
require 'northrays'

northrays = Northrays::Northrays.new
volume = northrays.volume.get('my-volume', create: true)
sandbox = northrays.create(
    Northrays::CreateSandboxFromSnapshotParams.new(
        volumes: [NorthraysApiClient::SandboxVolume.new(
            volume_id: volume.id,
            mount_path: '/home/northrays/data'
        )]
    )
)
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.northrays.sdk.Northrays;
import io.northrays.sdk.Sandbox;
import io.northrays.sdk.exception.NorthraysNotFoundException;
import io.northrays.sdk.model.CreateSandboxFromSnapshotParams;
import io.northrays.sdk.model.Volume;
import io.northrays.sdk.model.VolumeMount;

import java.util.Collections;

public class App {
    public static void main(String[] args) {
        try (Northrays northrays = new Northrays()) {
            Volume volume;
            try {
                volume = northrays.volume().getByName("my-volume");
            } catch (NorthraysNotFoundException e) {
                volume = northrays.volume().create("my-volume");
            }

            VolumeMount mount = new VolumeMount();
            mount.setVolumeId(volume.getId());
            mount.setMountPath("/home/northrays/data");

            CreateSandboxFromSnapshotParams params = new CreateSandboxFromSnapshotParams();
            params.setVolumes(Collections.singletonList(mount));

            Sandbox sandbox = northrays.create(params);
        }
    }
}
```

</TabItem>
<TabItem label="CLI" icon="seti:shell">

```shell
northrays volume create my-volume
northrays create --volume my-volume:/home/northrays/data
```

</TabItem>
<TabItem label="API" icon="seti:json">

```bash
curl 'https://app.northrays.com/api/sandbox' \
  --request POST \
  --header 'Authorization: Bearer <API_KEY>' \
  --header 'Content-Type: application/json' \
  --data '{
  "volumes": [
    {
      "volumeId": "<VOLUME_ID>",
      "mountPath": "/home/northrays/data"
    }
  ]
}'
```

</TabItem>
</Tabs>

### Create a sandbox with a Git repository cloned

Create a sandbox with a [Git repository](/docs/en/typescript-sdk/git) cloned to manage version control.

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```python
from northrays import Northrays

northrays = Northrays()
sandbox = northrays.create()

sandbox.git.clone("https://github.com/northrays/sandbox-platform.git", "/home/northrays/northrays")
status = sandbox.git.status("/home/northrays/northrays")
print(f"Branch: {status.current_branch}")
```

</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">

```typescript
import { Northrays } from '@northrays/sdk';

const northrays = new Northrays();
const sandbox = await northrays.create();

await sandbox.git.clone('https://github.com/northrays/sandbox-platform.git', '/home/northrays/northrays');
const status = await sandbox.git.status('/home/northrays/northrays');
console.log(`Branch: ${status.currentBranch}`);
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```go
package main

import (
    "context"
    "fmt"
    "log"

    "github.com/northrays/sandbox-platform/libs/sdk-go/pkg/northrays"
)

func main() {
    client, err := northrays.NewClient()
    if err != nil {
        log.Fatal(err)
    }

    sandbox, err := client.Create(context.Background(), nil)
    if err != nil {
        log.Fatal(err)
    }

    sandbox.Git.Clone(context.Background(), "https://github.com/northrays/sandbox-platform.git", "/home/northrays/northrays")
    status, err := sandbox.Git.Status(context.Background(), "/home/northrays/northrays")
    if err != nil {
        log.Fatal(err)
    }
    fmt.Printf("Branch: %s\n", status.CurrentBranch)
}
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">

```ruby
require 'northrays'

northrays = Northrays::Northrays.new
sandbox = northrays.create

sandbox.git.clone(url: "https://github.com/northrays/sandbox-platform.git", path: "/home/northrays/northrays")
status = sandbox.git.status("/home/northrays/northrays")
puts "Branch: #{status.current_branch}"
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.northrays.sdk.Northrays;
import io.northrays.sdk.Sandbox;
import io.northrays.sdk.model.GitStatus;

public class App {
    public static void main(String[] args) {
        try (Northrays northrays = new Northrays()) {
            Sandbox sandbox = northrays.create();
            sandbox.git.clone("https://github.com/northrays/sandbox-platform.git", "/home/northrays/northrays");
            GitStatus status = sandbox.git.status("/home/northrays/northrays");
            System.out.println("Branch: " + status.getCurrentBranch());
        }
    }
}
```

</TabItem>
<TabItem label="API" icon="seti:json">

```bash
# Create a sandbox
curl 'https://app.northrays.com/api/sandbox' \
  --request POST \
  --header 'Authorization: Bearer <API_KEY>' \
  --header 'Content-Type: application/json' \
  --data '{}'

# Clone a Git repository in the sandbox
curl 'https://proxy.app.northrays.com/toolbox/{sandboxId}/git/clone' \
  --request POST \
  --header 'Authorization: Bearer <API_KEY>' \
  --header 'Content-Type: application/json' \
  --data '{
  "url": "https://github.com/northrays/sandbox-platform.git",
  "path": "/home/northrays/northrays"
}'

# Get repository status
curl 'https://proxy.app.northrays.com/toolbox/{sandboxId}/git/status?path=/home/northrays/northrays' \
  --header 'Authorization: Bearer <API_KEY>'
```

</TabItem>
</Tabs>
